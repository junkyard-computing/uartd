// SPDX-License-Identifier: Apache-2.0
//
// uartd daemon entry point. Owns the serial port, captures continuously, serves the CLI
// over a Unix socket. Wired up in later milestones.

fn main() {
    eprintln!("uartd: not yet implemented");
    std::process::exit(1);
}
